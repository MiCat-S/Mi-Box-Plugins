'use strict';
const test=require('node:test'),assert=require('node:assert/strict'),path=require('node:path');const core=path.resolve(__dirname,'../../TeleBox-Core');const {buildPlugin}=require(path.join(core,'scripts/build-v2-plugin.cjs'));const create=require(path.join(buildPlugin({id:'his',packageRoot:path.resolve(__dirname,'../his'),entry:'v2.ts'}).artifactDir,'index.cjs')).default;
function fixture(items){const edits=[],replies=[];const client={async getEntity(value){return value==='7'?{id:-1007n,title:'Group',megagroup:true}:{firstName:'Alice'}},async *iterMessages(){for(const x of items)yield x}};const context={signal:new AbortController().signal,telegram:{edit:async(m,text,o)=>edits.push({text,o}),reply:async(m,text,o)=>replies.push({text,o}),getReply:async()=>({senderId:'42'}),withClient:fn=>fn(client,context.signal)}};return{edits,replies,run:args=>create().commands.his.handle({message:{id:1,chatId:'7',text:'.his',outgoing:true,replyToId:2,raw:{peerId:'7'}},args,command:'his',prefix:'.'},context)};}
test('his resolves reply target and renders media safely',async()=>{const f=fixture([{id:3,text:'<x>',media:{className:'MessageMediaPhoto'}}]);await f.run([]);assert.match(f.edits.at(-1).text,/Alice/);assert.match(f.edits.at(-1).text,/\[图片\] &lt;x&gt;/);});
test('his validates counts',async()=>{const f=fixture([]);await f.run(['@alice','0']);assert.equal(f.edits.at(-1).text,'❌ 无效的数量参数');});
