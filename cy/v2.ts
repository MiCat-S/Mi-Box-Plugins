import {renderHelp as renderPluginHelp} from "./v2/help";
import {readFile} from "node:fs/promises";
import {definePlugin,type PluginContext,type MessageEnvelope} from "telebox/sdk";
import type {Api} from "teleproto";
type State={schemaVersion:1;enabled:boolean;target:string;times:string[];limit:number;lastRunKeys:string[];importedLegacy:boolean;[key:string]:unknown};
const defaults:State={schemaVersion:1,enabled:false,target:"",times:[],limit:500,lastRunKeys:[],importedLegacy:false};
const store=(c:PluginContext)=>c.storage.json<State>("schedule.json",defaults),valid=(v:string)=>/^([01]\d|2[0-3]):[0-5]\d$/.test(v),limit=(v:unknown,f=500)=>Math.max(50,Math.min(2000,Number.isFinite(Number(v))?Math.floor(Number(v)):f));
const esc=(v:unknown)=>String(v??"").replace(/[&<>"']/g,x=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#x27;"})[x]!);
function normalize(v:any):State{return{...v,schemaVersion:1,enabled:v?.enabled===true,target:typeof v?.target==="string"?v.target.trim():"",times:Array.isArray(v?.times)?[...new Set(v.times.map(String).filter(valid))].slice(0,12):[],limit:limit(v?.limit),lastRunKeys:Array.isArray(v?.lastRunKeys)?v.lastRunKeys.map(String).slice(-40):[],importedLegacy:true};}
async function migrate(c:PluginContext){const cur=await store(c).read();if(cur.importedLegacy&&cur.schemaVersion===1)return;let source:any=cur;try{source={...JSON.parse(await readFile(c.files.dataPath("cy_schedule.json"),"utf8")),...cur};}catch{}await store(c).update(()=>normalize(source));}
const stop=new Set(["这个","那个","就是","不是","可以","没有","一个","什么","怎么","我们","你们","消息","回复","the","and","for","with","this","that","http","https"]);
function words(text:string,map:Map<string,number>){const cleaned=text.replace(/https?:\/\/\S+/gi," ").replace(/[^\p{Script=Han}a-zA-Z0-9_+.-]+/gu," ");for(const x of cleaned.match(/[a-zA-Z][a-zA-Z0-9_+.-]{1,24}|[\p{Script=Han}]{2,8}/gu)??[]){const key=x.toLowerCase();if(!stop.has(key)&&!/^[\d._+-]+$/.test(key))map.set(key,(map.get(key)??0)+1);}}
async function generate(c:PluginContext,target:any,count:number){return c.telegram.withClient(async(client,signal)=>{const map=new Map<string,number>();let valid=0;for await(const raw of client.iterMessages(target,{limit:count})){signal.throwIfAborted();const text=String(raw?.text??raw?.message??"").trim();if(!text||raw?.sticker)continue;valid++;words(text,map);}const entries=[...map].filter(([,n])=>n>=2).sort((a,b)=>b[1]-a[1]).slice(0,100);if(!entries.length)throw new Error("insufficient_words");
// canvas is a production runtime dependency; the plugin repository does not install its typings.
// @ts-expect-error resolved by the Core runtime dependency set
const canvas=await import("canvas");const cv=canvas.createCanvas(900,640),ctx=cv.getContext("2d");ctx.fillStyle="#fff";ctx.fillRect(0,0,900,640);ctx.textAlign="center";const max=entries[0]![1];entries.forEach(([word,n],i)=>{const size=Math.round(14+58*Math.sqrt(n/max));const cols=10,row=Math.floor(i/cols),col=i%cols;ctx.font=`700 ${size}px sans-serif`;ctx.fillStyle=["#0f766e","#166534","#1d4ed8","#0891b2","#ca8a04","#dc2626","#7c3aed"][i%7]!;ctx.fillText(word,55+col*88,70+row*52,82);});ctx.fillStyle="#111827";ctx.font="26px sans-serif";ctx.fillText(`最近 ${count} 条热词云 | ${valid} 条有效消息`,450,610);return cv.toBuffer("image/png");});}
async function send(c:PluginContext,target:any,count:number,replyTo?:number){const png=await generate(c,target,count);if(png.length>10*1024*1024)throw new Error("output_too_large");await c.telegram.withClient(async client=>{const {CustomFile}=await import("teleproto/client/uploads.js");await client.sendFile(target,{file:new CustomFile("cy-wordcloud.png",png.length,"",png),replyTo});});}
function status(s:State){return `词云定时：${s.enabled?"on":"off"}\n目标：${s.target||"未设置"}\n时间：${s.times.join(", ")||"未设置"}\n数量：${s.limit}`;}
export default function createCy(){return definePlugin({renderHelp: renderPluginHelp, apiVersion:1,id:"cy",description:"统计聊天热词并生成词云",commands:{cy:{helpArgs: ["help","?"], description:"立即或定时生成词云",ignoreEdited:true,async handle({message,args,prefix},c){const sub=args[0]?.toLowerCase(),state=normalize(await store(c).read());if(sub==="target"){const target=!args[1]||args[1]==="here"?message.chatId:args[1];await store(c).update(v=>normalize({...v,target}));await c.telegram.edit(message,`词云目标已设置：${esc(target)}`);return;}if(sub==="time"){const values=args.slice(1).flatMap(x=>x.split(",")),times=values.filter(valid),numeric=values.find(x=>/^\d+$/.test(x));if(!times.length||values.some(x=>!valid(x)&&!/^\d+$/.test(x))){await c.telegram.edit(message,`用法：${prefix}cy time 09:00,21:30 [数量]`);return;}const next=normalize({...state,times,limit:numeric?limit(numeric,state.limit):state.limit});await store(c).update(()=>next);await c.telegram.edit(message,status(next));return;}if(sub==="on"||sub==="off"){const next=normalize({...state,enabled:sub==="on"});if(next.enabled&&(!next.target||!next.times.length)){await c.telegram.edit(message,"请先设置目标和时间");return;}await store(c).update(()=>next);await c.telegram.edit(message,status(next));return;}if(sub==="status"||sub==="config"){await c.telegram.edit(message,status(state));return;}if(sub==="help"||sub==="?"){await c.telegram.edit(message,`<code>${prefix}cy [数量]</code>\n<code>${prefix}cy target here|目标</code>\n<code>${prefix}cy time 09:00,21:30 [数量]</code>\n<code>${prefix}cy on|off|status</code>`,{parseMode:"html"});return;}const count=limit(sub==="send"||sub==="now"?args[1]:sub,state.limit),target=sub==="send"||sub==="now"?(state.target||message.chatId):message.chatId;await c.telegram.edit(message,`正在统计最近 ${count} 条消息…`);try{await send(c,target,count,message.replyToId??message.id);await c.telegram.withClient(async client=>{const raw=message.raw as any;if(typeof raw?.delete==="function")await raw.delete({revoke:true});});}catch{if(!c.signal.aborted)await c.telegram.edit(message,"没有统计到足够的热词，或词云生成失败");}}}},jobs:{scheduled_cloud:{description:"发送定时词云",cron:"* * * * *",timeZone:"Asia/Shanghai",async handle(c,signal){const state=normalize(await store(c).read());if(!state.enabled||!state.target||!state.times.length)return;const parts=new Intl.DateTimeFormat("sv-SE",{timeZone:"Asia/Shanghai",year:"numeric",month:"2-digit",day:"2-digit",hour:"2-digit",minute:"2-digit",hourCycle:"h23"}).formatToParts();const get=(t:string)=>parts.find(x=>x.type===t)?.value??"";const time=`${get("hour")}:${get("minute")}`,key=`${get("year")}-${get("month")}-${get("day")}:${time}`;if(!state.times.includes(time)||state.lastRunKeys.includes(key))return;signal.throwIfAborted();await send(c,state.target,state.limit);await store(c).update(v=>normalize({...v,lastRunKeys:[...normalize(v).lastRunKeys,key].slice(-40)}));}}},settings:c=>({id:"cy",title:"词云",description:"词云定时任务配置",category:"插件配置",icon:"☁️",getSchema:()=>[{key:"enabled",label:"启用定时",type:"boolean"},{key:"target",label:"目标聊天",type:"string"},{key:"times",label:"执行时间",type:"json"},{key:"limit",label:"消息数量",type:"number",min:50,max:2000}],getValues:()=>store(c).read(),setValues:async patch=>{await store(c).update(v=>normalize({...v,...patch}));}}),setup:migrate});}
